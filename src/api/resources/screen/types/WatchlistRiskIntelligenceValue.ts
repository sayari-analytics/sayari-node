/**
 * This file was auto-generated by Fern from our API Definition.
 */

import * as Sayari from "../../../index";

/**
 * A single risk intelligence record. Unlike other attribute records, this type does NOT contain a "value" field. Risk label information is in the "list" and "program" fields.
 */
export interface WatchlistRiskIntelligenceValue {
    /** Name of the sanctions or risk list (e.g. "OFAC Specially Designated Nationals (SDN) List"). */
    list?: string;
    /** Risk category (e.g. "sanctions", "export_controls", "pep"). */
    type?: string;
    /** Risk score, if provided by the source list. */
    score?: number;
    /** Reason for designation or listing. */
    reason?: string;
    /** Sanctions program or sub-program name (e.g. "CUBA", "IRAN"). */
    program?: string;
    /** Issuing authority (e.g. "OFAC", "Department of the Commerce - International Trade Administration"). */
    authority?: string;
    /** Date the listing took effect (ISO 8601 date string). */
    fromDate?: string;
    /** Date the listing was removed, if applicable. */
    toDate?: string;
    /** Generic date field for sources that do not distinguish from/to dates. */
    date?: string;
    /** Supplemental metadata as key-value pairs (e.g. license requirements, federal register notices). */
    extra?: Sayari.WatchlistKeyValue[];
}
