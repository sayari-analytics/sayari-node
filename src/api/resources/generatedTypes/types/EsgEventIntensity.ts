/**
 * This file was auto-generated by Fern from our API Definition.
 */

/**
 * Used to assess the severity or impact of a specific Environmental, Social, and Governance (ESG) event on a company, normalizing vendor-specific scoring systems (like SESAMm's 1-5 scale) into a standard scale.
 */
export type EsgEventIntensity =
    /**
     * Denotes a severe ESG risk event with significant impact to the entity's reputation, stakeholders, or financial standing (e.g., SESAMm intensity scores of 4 or 5). */
    | "high"
    /**
     * Denotes a minor ESG risk event with minimal or localized impact (e.g., SESAMm intensity score of 1) */
    | "low"
    /**
     * Denotes a moderate ESG risk event with noticeable but manageable impact (e.g., SESAMm intensity score of 2 or 3). */
    | "medium";
export const EsgEventIntensity = {
    High: "high",
    Low: "low",
    Medium: "medium",
} as const;
